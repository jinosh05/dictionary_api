name: dictionary_api
description: A Flutter Project for calling dictionary_api
publish_to: "none"
version: 1.0.1+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.5.0+1
  # provider: ^6.0.5
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  meta: ^1.12.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
